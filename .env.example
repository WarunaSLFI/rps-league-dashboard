# Server
PORT=3001

# Legacy RPS API
RPS_API_BASE_URL=https://bad-api-assignment.reaktor.com
RPS_API_TOKEN=

# Pagination safety cap (max pages fetched per request)
MAX_PAGINATION_PAGES=10

# Client (Vite prefix required for client-side access)
VITE_API_URL=http://localhost:3001
